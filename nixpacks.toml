providers = ["python"]

[setup]
aptPkgs = ["gcc", "g++", "libffi-dev", "libssl-dev", "python3-dev"]

[install]
cmd = "python -m pip install --upgrade pip && pip install -r agents/requirements.txt"

[start]
cmd = "python -m agents.worker start"
